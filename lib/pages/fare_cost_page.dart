import 'package:flutter/material.dart';

class FareCostPage extends StatelessWidget {
  const FareCostPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Fare Cost'),
    );
  }
}
